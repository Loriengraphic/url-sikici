const { GuildMember, MessageEmbed,Client} = require("discord.js");
const request = require('request')
const ms = require('ms');
module.exports = {
    amcık: "ready",
    yarrak: "true",
    Isim: "Test Event",
    /**
     * @param {Client} client
     */
    sikiminbasi: function (client) {
      // setInterval(() => {
      // 
      // }, 10000)
    },
     /**
     * @param {client} ready
     */
    ananınamı: async function () {

        },
  };